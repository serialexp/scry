# v1.0 — Web UI (own observability frontend) — Design

Status: partial — Phase 0 (shell) + Phase 1 (Explore rebuild) + Phase 1a (Metrics Explore) + Phase 2 (Fleet) landed; Phases 3–4 outstanding
Owner: Bart
Last updated: 2026-08-20

> This is the **v1.0 "own UI"** milestone named in `README`/`CLAUDE.md`
> as "the next round". It supersedes the Grafana-adapter direction: scry
> grows its own operator frontend instead of speaking Grafana's
> datasource protocols. PromQL stays demoted (D — "own UI removes the
> Grafana-compat driver"); the metrics view queries through scry's own
> wire, not PromQL.
>
> The visual target is a Claude Design mock (`scry - Redesign.dc.html` +
> `scry-source/`) — a **picture to build toward**, not portable code. It
> is authored in Claude's `dc-runtime` template DSL (`sc-for`/`sc-if` +
> a `DCLogic` class of hardcoded mock data); nothing in it compiles into
> our stack. This doc is the contract for turning that picture into the
> real SolidJS app.

## Implementation status

Tracking the gap between this design and what's on the main branch.
Tick items as they land; move whole sections to "done" once everything
inside them is checked.

### Done

- [x] **Decision — dashboards persistence.** Object store is the source of
  truth (durable JSON under a reserved prefix); a catalog table is the
  runtime-lookup index. Not Valkey.
- [x] **Decision — alerts scope.** Defer the alerting engine entirely. Ship the
  Alerts *view* inert, gated behind a "no backend support yet" state. No
  server-side rule storage/evaluation in v1.0.
- [x] **Decision — fleet requires Valkey.** No single-instance fleet fallback.
  A Valkey-less queryd refuses `FleetStatusRequest` with a `StreamError`
  ("fleet unavailable") — a lone queryd knows nothing about other instances,
  so "fleet" would be a misnomer.
- [x] **Phase 0 — design system + app shell.** Design tokens (warm-dark canvas,
  amber/teal accents, level colors, IBM Plex Mono) in `desktop/src/styles.css`;
  `@solidjs/router` added; `App.tsx` is now a `HashRouter` shell with brand +
  Explore/Dashboards/Alerts/Fleet nav + version + logout, gated behind the
  existing auth. Explore holds the unchanged query UI (`views/Explore.tsx`);
  Dashboards/Fleet are placeholders, Alerts is the inert "no backend yet" view.
  Query path untouched — `smoke-webui.sh` green (13/13), typecheck + build
  clean. (Target selector stays in `QueryForm` for now; moving it into the
  shell is folded into Phase 1.) Font woff2 bundling deferred (graceful mono
  fallback until then).
- [x] **Phase 1 — Explore rebuild.** Re-skinned the query app to the mock's
  Explore shape: a horizontal query bar (`components/explore/QueryBar.tsx`)
  with signal tab pills (Logs/Traces/Metrics/Profiles), a token-styled matcher
  bar (teal key / amber value), quick time-range pills, and an Advanced popover
  holding the raw controls (SQL / limit / trace-id / custom bounds / frames
  overview); a fields strip + full-field browser overlay with per-value
  top-values bars (`components/explore/FieldsStrip.tsx`, rehousing the D-050
  label discoverability out of the old sidebar); a right-hand inspector rail
  showing the selected log's fields + raw event + trace jump
  (`components/explore/InspectorRail.tsx`); logs re-rendered as a compact
  Time/Level/Service/Message/Trace grid with click-to-inspect + row selection
  (`components/ResultsTable.tsx`). The connection target moved into the shell
  topbar (`components/ConnectionPicker.tsx`) — folding in the Phase 0 deferral.
  Quick-range + active-range + inspector-selection state centralized in the
  store (Rule #9). Reused the existing store, transport, and result readers —
  no protocol change. `smoke-webui.sh` green (13/13), typecheck + build clean,
  visual render confirmed in-browser.
- [x] **Phase 1a — Metrics Explore.** The mock's Metrics tab: a metric-name
  picker (backed by the existing `__name__` label-values metadata request), an
  aggregation control (avg/sum/min/max/count), and a **per-series toggle**, over
  a uPlot time-series line chart. New `desktop/src/metricsChart.ts` (pure decode
  + palette + `labelsToName`), `components/explore/MetricsPanel.tsx` (uPlot lines
  mirroring `VolumePanel`'s ResizeObserver + destroy/rebuild + drag-to-zoom), and
  a metrics-chart block in `store.ts` (signals `metricAgg`/`metricGrouped`/
  `metricsChartData`, actions `setMetricName`/`setMetricAgg`/`setMetricGrouped`/
  `runMetricsChart`, wired into `runCurrentQuery`/`applyQuickRange`). Server-side
  `date_bin` downsample (reused from the log-volume machinery) keeps dense raw
  samples chartable. **Per-series legends carry real labels** via D-058
  (`with_labels` — a best-effort companion query resolves each
  `series_fingerprint` → `{k="v", …}`, with fingerprint-hex fallback on any
  error), so the interim "opaque fingerprint" plan was superseded before it
  shipped. No wire change from the frontend beyond opting into the already-landed
  `with_labels` byte. Typecheck + build clean; e2e-verified in-browser against a
  metrics-populated Garage bucket (aggregated line, 24 label-legended per-series
  lines, select-reset fix).
- [x] **Dependency for 1a — D-058 fingerprint→label resolution (another agent).**
  The `with_labels` opt-in byte on `QueryRequest`: for metrics, the server
  synthesizes a `labels` `Map<Utf8,Utf8>` column by inverting the postings
  sidecars per resolved fingerprint. Off by default. This removed the need for
  the "separate feature request" the Phase 1a plan had carved out — real series
  names are available now, not deferred.
- [x] **Phase 2 — Fleet view + status forwarding.** `FleetStatusRequest` /
  `FleetStatusResponse` frames use the existing query transport. A
  Valkey-connected queryd injects its `FleetSource` independently of the optional
  `--stats-listen` endpoint, validates and forwards the live `StatusSnapshot`
  documents, while a Valkey-less queryd returns `QUERY_ERR_FLEET_UNAVAILABLE`.
  scry-webui remains an authenticated byte-pipe. The SolidJS Fleet route polls
  every five seconds, groups agent/ingest/query cards, and supports browser and
  Tauri transports.

### Outstanding

- [ ] **Phase 3 — Dashboards.** Panel model; object-store persistence (durable
  JSON) + a catalog index table for runtime lookup; the dashboard grid/tree UI.
  Read-only render first, then editing.
- [ ] **Phase 4 — Alerts (inert).** Ship the Alerts *view* only, gated behind a
  "no backend support yet" empty/locked state. The evaluation engine (rule
  storage, periodic eval, ok/pending/firing, notifiers) is **deferred** to a
  later milestone — not v1.0.
- [x] **Tauri parity for Fleet.** Fleet uses the shared query-protocol client,
  so the Tauri transport talks directly to the selected Valkey-connected queryd
  while the browser continues through scry-webui.
- [ ] **Open — auth surface.** The webui password/session model is unchanged;
  confirm Fleet/Dashboards/Alerts don't need a finer-grained authz story for
  v1.0.

## Why this exists

Today scry has **two disconnected frontends**:

1. The **query app** (`desktop/`, SolidJS + Vite, dual-mode Tauri/browser)
   — a single-screen logs/traces query UI served in-browser by
   `scry-webui` (rust-embed bundle) or natively by Tauri. No metrics view,
   no dashboards, no alerts, no fleet view.
2. The **operator status pages** (`crates/server/src/stats.rs`) — hand-rolled
   inline HTML/CSS/JS served by every `scry ingest` / `scry query` daemon on
   `--stats-listen` (default `:4098`), rendering the Valkey-aggregated fleet
   (D-057).

The Claude Design mock unifies these into **one product UI with four views** —
Explore (query), Dashboards, Alerts, Fleet status — in a single dark,
operator-grade design language. This doc is the contract for building that as
the real app.

**Decided (with Bart):**

- **Full v1.0 UI** — all four views, built as the SolidJS app in `desktop/`
  and served to browsers by `scry-webui`. This is purely a webui + desktop
  effort.
- **The daemons' standalone `stats.rs` pages stay untouched.** They remain the
  zero-dependency, self-served operator page on `:4098`. The new Fleet view is
  a *separate* rendering that lives only in the SPA.
- **Fleet data path:** the new UI asks **queryd** for status; queryd forwards
  the data it already pulls from Valkey via `discover_status_blobs`. scry-webui
  stays a dumb byte-pipe. No new Valkey dependency is added to scry-webui, and
  the browser never talks to Valkey.

## Goals

- One unified SolidJS app covering Explore, Dashboards, Alerts, Fleet.
- Match the mock's design language: near-black canvas (`#14120e`), amber
  accent (`#e8a33d`), teal secondary (`#5bb6a8`), IBM Plex Mono for data /
  system-sans for chrome.
- Reuse the existing wire, transport abstraction, store discipline (SolidJS
  stores, **no prop drilling** — Rule #9), and Arrow result readers. Views are
  presentation on top of the current protocol wherever possible.
- Fleet status reachable in-browser without any new browser→Valkey path:
  queryd is the only Valkey talker; the UI talks only to queryd (via the
  webui relay).
- Preserve dual-mode (Tauri native + browser) for the query views.

## Non-goals (v1.0)

- **No change to the daemons' `stats.rs` pages.** They keep serving `:4098`
  independently; this doc does not touch them.
- **No PromQL.** The Metrics view queries through scry's own wire (matchers +
  time range → series), not a PromQL engine. (PromQL stays demoted.)
- **No Grafana datasource adapters.** The own-UI direction replaces them.
- **No new browser→Valkey or browser→object-store path.** All data flows
  through the queryd wire and the webui relay.
- **Alerts *evaluation* is not committed for v1.0** until its scope is decided
  (see Open questions). The Alerts *view* may ship read-only or be deferred.

## Design language (from the mock)

| Token | Value | Use |
|---|---|---|
| canvas | `#14120e` / `#16140f` / `#111009` | app bg, chrome, panels |
| border | `#2b2822` / `#38342b` | separators, input borders |
| accent | `#e8a33d` (amber) | primary actions, "Run", brand, selection |
| secondary | `#5bb6a8` (teal) | query keys, INFO level |
| text | `#f0ece3` / `#9a9385` / `#6f6a5e` | primary / muted / faint |
| levels | ERROR `#e5695f` · WARN `#e8a33d` · INFO `#5bb6a8` · DEBUG `#8f8a7d` |
| mono | IBM Plex Mono (bundled woff2) | all data, timestamps, ids |

These become CSS variables in `styles.css` (extending the existing dark theme),
not per-element inline styles (the mock inlines everything; we don't).

## App architecture

The app stays in **`desktop/`** (SolidJS + Vite; Vite is esbuild-backed — the
"compiled toolchain" is already in place) and is served three ways, unchanged:

- **browser** → `scry-webui` serves the rust-embed bundle; `HttpTransport`
  POSTs framed bytes to `/api/query`, target chosen by `X-Scry-Target` id.
- **Tauri** → native TCP via `TauriTransport` (`run_query` invoke).
- selection is the existing lazy `isTauri()` dynamic-import in `store.ts`.

### Routing

Introduce **`@solidjs/router`** and convert `App.tsx` from a single screen to a
routed shell:

```
/            → redirect to /explore
/explore     → Explore (Logs/Traces/Metrics)   [existing query app, re-skinned]
/dashboards  → Dashboards
/alerts      → Alerts
/fleet       → Fleet status
```

The top nav (mock's topbar: brand + nav pills + version + avatar) is the router
shell; the target `<select>` (browser mode) moves into the shell. Each route is
a lazy-loaded view so the Explore path stays light.

### State

Keep the centralized-store discipline (Rule #9). The current `store.ts` is the
Explore store; add **per-view stores** (`store/fleet.ts`, `store/dashboards.ts`,
…) rather than one mega-store, each `createStore` + action functions imported
directly by that view's components. Arrow tables stay in signals, never in a
proxied store (existing constraint).

## View 1 — Explore

The current query app, re-skinned to the mock. Existing pieces map directly:

| Mock element | Existing component | Change |
|---|---|---|
| Logs/Traces/Metrics tab pills | (new) signal tab | drives `state.signal` |
| token-styled query bar | `QueryForm.tsx` | present matchers as colored k/op/v tokens |
| time-range pills | `QueryForm` bounds | 5m/15m/1h/… presets → `tsMin`/`tsMax` |
| field-browser overlay | `LabelBrowser.tsx` | overlay/rail styling; reuse label fetch |
| results (logs reader) | `ResultsTable.tsx` | re-skin rows/badges |
| inspector rail (log/span detail) | expander in readers | promote to a right rail |
| volume histogram | `VolumePanel.tsx` (uPlot) | re-skin |
| traces waterfall / frames | `TracesView`/`FramesView` | re-skin |

No protocol change — this is presentation. The wire already carries logs,
traces, and metrics via `QueryRequest` (matchers + SQL + time bounds + Arrow
IPC results).

### View 1a — Metrics Explore

The mock's Metrics tab adds a metric picker + aggregation + a time-series chart.
Metrics already flow through the query wire (signal=metrics). Work is:
a metric-name picker (backed by the existing label-values metadata request for
`__name__`), and a uPlot **series** chart (we currently only chart log volume).
Confirm the query result shape supports one line per series over time; if a
server-side downsample/step is needed, note it here before building.

**Landed (2026-07-13).** The confirmed shape and what shipped:

- The `metrics` SQL table is exactly `(series_fingerprint: UInt64,
  ts_unix_nano: UInt64, value: Float64)`. A **server-side `date_bin` downsample
  is required** — raw samples are dense — so the chart query buckets to
  `TARGET_BUCKETS` (~120) with the same `chooseStepMs`/`stepIntervalSql` helpers
  the log-volume panel uses. Aggregated: `SELECT CAST(date_bin(INTERVAL 'N
  seconds', to_timestamp_nanos(ts_unix_nano)) AS BIGINT) AS bucket_ns,
  <agg>(value) AS v FROM metrics GROUP BY bucket_ns ORDER BY bucket_ns`;
  per-series adds `series_fingerprint AS fp` + `GROUP BY fp, bucket_ns`.
- **Series identity is now real labels, not opaque fingerprints.** The Phase 1a
  plan had assumed the fingerprint→labelset mapping was unavailable and carved
  out a "separate feature request" with a fingerprint-hex interim legend. That
  feature landed independently as **D-058** (`with_labels`) before Phase 1a
  shipped, so per-series mode issues a **best-effort companion query** (`SELECT
  series_fingerprint AS fp, labels FROM metrics LIMIT 20000` with
  `with_labels`) and joins each fingerprint to its `{k="v", …}` label set
  client-side (`decodeSeriesNames`), falling back to fingerprint-hex only on
  error. DataFusion can't `GROUP BY` a Map column, so the numeric values and the
  names are two queries joined by fingerprint on the client — not one grouped
  query.
- Aggregation doubles as the intra-bucket reducer (`count` → `count(value)`).
  The raw `(fingerprint, ts, value)` table still renders below via the generic
  `ResultsTable`, useful as the underlying data.

## View 4 — Fleet status (data path)

The one view that needs new **backend** wiring. The browser must not talk to
Valkey; queryd forwards what it already reads.

```
browser (SolidJS Fleet view)
  │  framed FleetStatusRequest  (X-Scry-Target: <queryd id>)
  ▼
scry-webui  POST /api/query   ── dumb byte-pipe, unchanged ──▶ queryd
                                                                 │ discover_status_blobs(valkey)
                                                                 ▼
                                            FleetStatusResponse { blobs: [json…] }
```

### Wire protocol

Add two variants to the query `QueryFrame` union (`proto/query.schema.json`,
regenerate with `scripts/gen-proto.sh` → `generated_query.rs` + the vendored TS
`generated.ts`):

- `FleetStatusRequest` (tag `0x04`) — client→queryd. Empty body (or an optional
  `role` filter later).
- `FleetStatusResponse` (tag `0x22`) — queryd→client. Carries the array of
  opaque status-JSON blobs (the exact `Vec<String>` `discover_status_blobs`
  returns — the same `StatusSnapshot` JSON the `stats.rs` page renders), plus
  `source` (`valkey`|`local`) and `self_id`.

Modeling it as a **new frame on the existing query wire** (not a new port, not
a new HTTP endpoint) means scry-webui's relay and the browser transport need
**zero** changes — the browser frames a request, the relay pipes bytes, queryd
answers. Only the schema + queryd handler + a TS client function are new.

### queryd handler

queryd already holds a `ValkeyClient` and constructs a `ValkeyFleetSource`
(D-057). To answer the new frame, inject a `FleetSource` into `QueryService`
(mirroring `with_live_discovery` / `with_metrics`) and handle
`FleetStatusRequest` in the connection dispatch: call `blobs()`, reply one
`FleetStatusResponse`. When queryd has **no Valkey**, it **refuses** with a
`StreamError` ("fleet unavailable") — decided, not a `source:local` fallback. A
lone Valkey-less queryd knows nothing about any other instance, so a
single-instance "fleet" would be a misnomer; the UI surfaces the refusal as
"fleet requires Valkey" rather than pretending a one-node fleet. (Mirrors the
D-054 `--live` refuse decision.)

### SolidJS Fleet view

`store/fleet.ts`: a `FleetStatusRequest` sender (reusing `Transport` +
framing), polled every ~1s; parse the JSON blobs into typed snapshots; group
into Ingest/Query sections; badge the `self_id` entry. Rates (bytes/s, records/s)
are computed client-side by diffing successive polls keyed on `instance_id` —
the same technique the `stats.rs` dashboard already uses, and the same
distinct-snapshot diffing the D-057 flicker fix landed (heartbeat republishes
only every ~2s, so diff against the last *distinct* `now_unix_ms`).

## View 2 — Dashboards (Phase 3)

New. A dashboard is a named set of panels; each panel is a saved query (signal +
matchers + viz type) laid out on a grid, with a tree/folder sidebar (per mock).

**Persistence (decided):** the **object store is the source of truth** — each
dashboard is durable JSON under a reserved prefix (e.g. `_dashboards/<id>.json`,
alongside the reserved `_catalog/` prefix; must be skipped by
`reconcile_from_bucket`/`poll_once`/`full_walk` like `_catalog/` is). A
**catalog table** (`dashboards(id, name, folder, updated_at, …)`) is the
runtime-lookup index so listing/searching doesn't walk the bucket — the same
object-store-truth + SQLite-index split the block catalog already uses.
Multi-instance writes converge through the object store (last-writer-wins on
`<id>.json`); the index is rebuilt from the bucket on reconcile. Ship read-only
render first (list + view saved dashboards), then editing. No server-side query
change — panels reuse `QueryRequest`.

## View 3 — Alerts (Phase 4 — inert UI, engine deferred)

The mock shows a rule list, a rule detail (condition + thresholds), and
notification targets. This implies an **alerting subsystem** scry does not have:
rule storage, periodic evaluation against the query engine, a state machine
(ok/pending/firing), and notifier integrations — a backend milestone in its own
right.

**Decided:** the alerting *engine* is **deferred** past v1.0. We ship the Alerts
*view* in the nav so the shape is present, but **inert** — gated behind an
explicit "no backend support yet" empty/locked state (no rule storage, no
evaluation, no live data). This keeps the four-view layout complete without
committing to the engine. When the engine milestone lands, this view wires up to
it; until then it is a deliberate placeholder, not a half-built feature.

## Files touched (by phase)

- **Phase 0/1** — `desktop/src/`: `App.tsx` (router shell), new `styles.css`
  tokens, `@solidjs/router` dep, re-skinned `components/*`, new nav; no Rust.
- **Phase 2** — `proto/query.schema.json` + `scripts/gen-proto.sh` output
  (`crates/proto/src/generated_query.rs`, `desktop/src/proto/generated.ts`);
  `crates/server/src/query_service.rs` (`with_fleet_source` + handler);
  `crates/scry-queryd/src/lib.rs` (inject the existing `ValkeyFleetSource` into
  `QueryService`); `desktop/src/store/fleet.ts` + `components/FleetView.tsx`. No
  scry-webui change.
- **Phase 3/4** — TBD per open questions.

## Open questions for review

**Resolved (see Implementation status § Done):**

- ~~**Dashboards persistence.**~~ → object store is truth (reserved prefix JSON),
  catalog table is the runtime index. Not Valkey.
- ~~**Alerts scope.**~~ → engine deferred; ship the view inert ("no backend
  support yet").
- ~~**Fleet-without-Valkey behavior.**~~ → refuse with `StreamError` "fleet
  unavailable"; no single-instance fallback.
- ~~**Metrics query shape.**~~ → a server-side `date_bin` downsample **is**
  required (dense raw samples); series identity resolves to real labels via
  D-058 `with_labels` (a companion fingerprint→labels query, joined client-side).
  Settled in Phase 1a — see View 1a § Landed.

**Still open:**

- **Tauri parity for Fleet.** Ship Fleet in the desktop build (needs a
  Valkey-connected queryd upstream) or make it browser-only?
- **Auth surface.** Confirm Fleet/Dashboards/Alerts don't need finer-grained
  authz than the existing single-password webui session for v1.0.

## References

- Design mock: `scry - Redesign (standalone).html`, `scry-source/scry - Redesign.dc.html`
- D-057 (status pages + Valkey fleet aggregation) — `docs/decisions.md`
- D-040 / D-046 (scry-webui relay, multi-target) — `docs/decisions.md`
- D-052 / D-053 / D-054 (tail + live query + Valkey discovery precedents) — `docs/decisions.md`
- Existing frontend: `desktop/src/` (SolidJS + Vite), `crates/scry-webui/`
