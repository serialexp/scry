/target
docker/garage/.env

# Profiler output (scripts/profile-query.sh + cargo-flamegraph).
/flamegraphs/
perf.data
perf.data.old

# Claude Design mock artifacts (visual target for the v1.0 web UI — see
# docs/design/v1.0-web-ui.md). Large generated bundles / design inputs, kept
# local, not tracked.
/scry - Redesign (standalone).html
/scry-source/
