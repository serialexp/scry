/// <reference types="vite/client" />

// Injected by Vite's `define` at build time from src-tauri/tauri.conf.json's
// `version` (see vite.config.ts). Available in both the Tauri and browser
// bundles as a plain string literal substituted at compile time.
declare const __APP_VERSION__: string;
